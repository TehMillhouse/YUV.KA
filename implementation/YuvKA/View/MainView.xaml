<Window x:Class="YuvKA.View.MainView"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		xmlns:cal="clr-namespace:Caliburn.Micro;assembly=Caliburn.Micro"
		xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
		xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
		mc:Ignorable="d"
		xmlns:my="clr-namespace:YuvKA"
		Title="Yuv.KA" Height="500" Width="700" cal:Bind.AtDesignTime="True" d:DataContext="{x:Static my:AppBootstrapper.DesignRoot}">
	<DockPanel>
		<DockPanel DockPanel.Dock="Top">
			<StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
				<Button x:Name="Open">Open</Button>
				<Button x:Name="Save">Save</Button>
				<Button x:Name="Clear">Clear</Button>
			</StackPanel>
			<ContentControl cal:View.Model="{Binding ReplayStateViewModel}"/>
		</DockPanel>
		<ContentControl cal:View.Model="{Binding ToolboxViewModel}" DockPanel.Dock="Bottom"/>
		<ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
			<ContentControl cal:View.Model="{Binding PipelineViewModel}"/>
		</ScrollViewer>
	</DockPanel>
</Window>

<UserControl x:Class="YuvKA.View.PropertyEditor.Implementation.DoublePropertyView"
			xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			mc:Ignorable="d"
			d:DesignHeight="35" d:DesignWidth="150">
	<StackPanel>
		<TextBlock Text="{Binding Property.DisplayName}" HorizontalAlignment="Center" FontSize="10"/>
		<!-- TODO: make this a grid so things overlap. That might save us some space -->
		<DockPanel>
			<TextBlock Text="{Binding Minimum}" DockPanel.Dock="Left" Width="10"/>
			<TextBlock Text="{Binding Maximum}" DockPanel.Dock="Right" Width="10"/>
			<Slider Value="{Binding Value}" Maximum="{Binding Maximum}" Minimum="{Binding Minimum}"/>
		</DockPanel>
		<TextBlock Text="{Binding Value}" HorizontalAlignment="Center" FontSize="10"/>
	</StackPanel>
</UserControl>
